Python -->
class Solution:
    def reverseString(self, s: str) -> str:
        return s[::-1]

c-->
void reverseString(char* str) {
    int n = strlen(str);
    for (int i = 0; i < n / 2; i++) {
        char temp = str[i];
        str[i] = str[n - i - 1];
        str[n - i - 1] = temp;
    }
}
